TODO
数据转化
1. 新增列，标记每一个分子是单体还是二聚体有没有环化等等；用 True False 标记
2. 转化 label，为了简单起见，先把各种 label 都统一到 min 为单位
数据可视化
1. 专利内-单体\二聚体
  1. 对于每一个专利，分别可视化单体和二聚体的 label 分布
  2. 对于每一个专利，分别可视化单体和二聚体的 feature 分布
2. 专利间
  1. feature
    1. 对于每一个数据点，用它的 feature 作降维
    2. 点的大小和 label 有关，半衰期越大点的半径越大
    3. 点的颜色和专利有关，不同的专利应该使用不同的颜色
  2. Label
    1. 绘制小提琴图
    2. 横轴是不同的专利
    3. 纵轴是标签的分布情况
简单模型验证
1. 对每一个专利文件，进行二分类转化，可以根据已经统一到 min 的 label 的分布情况，在每一个专利内部根据中位数来确定正样本和负样本；也就是样本是稳定还是不稳定
2. 根据“稳定”还是“不稳定”，作为二分类标签，验证简单模型的效果
3. 验证包括
  1. 交叉验证
  2. 指标重要性评估
  3. 模型迁移
数据说明
US9809623B2-JiananLiWO2017011820A2-LiJiananUS20140294902A1-LiJiananUS9624268sif_sgf_second
1. us980923b2
  1. 已经使用 min 作为label
2. wo2017011820A2
  1. 部分使用*作为label；部分使用 min
  2. S is the matrix used is 100 fold dilution of standard SIF concentraticon
  3. *=>360min; **=180-360minn; ***=120-180min; ****=<60-120min; *****=<60min
3. US20140294902A1
  1. 已经使用 min 作为 label
4. US9624268
  1. 使用*作为 label
  * = >360 min;
  ** = 180-360 min;
  *** = 120-180 min;
  **** = <60-120 min;
  ***** = <60 min
5. sif_sgf_second
  1. 使用数字 1-4 作为标记
  4: > 2 hours
  3: 1.5–2 hours
  2: 1–1.5 hours
  1: < 1 hour